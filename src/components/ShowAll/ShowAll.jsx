/* eslint-disable react/prop-types */

function ShowAll({ children }) {
  return (
    <div className="">
      <button className="btn btn-success btn-sm">{children}</button>
    </div>
  );
}

export default ShowAll;
